<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.muziuniv_be_user.application.account.AccountMapper">

    <select id="findByUserInfo">
        SELECT u.user_id, u.user_role, u.user_name, u.password,
               u.login_id, d.dept_name
        FROM user u
        LEFT JOIN student s ON u.user_id = s.user_id
        LEFT JOIN professor p ON u.user_id = p.user_id
        LEFT JOIN staff sf ON u.user_id = sf.user_id
        LEFT JOIN department d ON d.dept_id = COALESCE(s.dept_id, p.dept_id)
        <where>
            u.login_id = #{loginId}
        </where>

    </select>

    <select id="selectMyPrivacy">
        SELECT login_id, user_name, address, phone, email, add_detail
        FROM user
        WHERE user_id = #{userId}
    </select>

    <update id="updateMyPrivacy">
        UPDATE user
        <set>
            <if test="address != null and address != ''" >address = #{address},</if>
            <if test="addDetail != null and addDetail != ''">add_detail = #{addDetail},</if>
            <if test="phone != null and phone != ''">phone = #{phone},</if>
            <if test="email != null and email != ''"> email = #{email},</if>
        </set>
        WHERE user_id = #{userId}
    </update>

    <update id="updateMyPwd">
        UPDATE user
        SET password = #{password}
        WHERE email = #{email}
    </update>
</mapper>

