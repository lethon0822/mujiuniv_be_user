constants:
  file:
    directory: /home/download
  jwt:
    refresh-token-cookie-path: /mujiuniv-api/api/auth/reissue

eureka:
  client:
    enabled: false

server:
  port: 8080

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: health

spring:
  cloud:
    kubernetes:
      discovery:
        enabled: true
      reload:
        enabled: true
        strategy: refresh
  datasource:
    url: jdbc:log4jdbc:mariadb://192.168.0.39/final_project_user
  jpa:
    hibernate:
      ddl-auto: update